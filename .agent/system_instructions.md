# System Instructions

> **All authoritative instructions are now in `CLAUDE.md` (project root).**
> This file is retained only for backward compatibility with task files that reference it.
> Do NOT add new content here — update CLAUDE.md instead.

## Task File Reference

All task definitions are in `.agent/tasks/`. All workflow entry points are in `.agent/workflows/`.
See CLAUDE.md "Workflow Quick Reference" for the complete trigger → task file mapping.
