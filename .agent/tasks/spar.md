# Task: Spar (Alias)
# Trigger: "spar [topic]", "spar [[Note]]"

> This is an alias for `session.md` that starts in **adversarial mode**.

Execute `tasks/session.md` with the spar stance as the default starting mode. All other behavior is identical — the session can still shift to co-create as needed.
